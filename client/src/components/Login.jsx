import React from "react";
import { Router, Route } from "react-router";

const Login = props => {
  return (
    <div>
      <input type="text" />
      <input type="text" />
      <button>login</button>
    </div>
  );
};

export default Login;
